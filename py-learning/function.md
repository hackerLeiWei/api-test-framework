# 声明
```Python
def demo(pos1, pos2, /, normal1, normal2, *args, kw1, kw2, **kwargs):
    """
    参数拆解：
    - pos1, pos2：在 / 左边 → 仅限位置
    - normal1, normal2：在 / 和 * 之间 → 普通（位置或关键字皆可）
    - args：捕获多余的位置参数
    - kw1, kw2：在 * 右边 → 仅限关键字
    - kwargs：捕获多余的关键字参数
    """
    print(f"仅限位置: {pos1}, {pos2}")
    print(f"普通参数: {normal1}, {normal2}")
    print(f"多余位置: {args}")
    print(f"仅限关键字: {kw1}, {kw2}")
    print(f"多余关键字: {kwargs}")


# ---------- 正确的调用方式 ----------
demo(
    1,2,  # pos1, pos2 必须按位置
    3,4,  # normal1, normal2 可以按位置
    5,
    6,
    7,  # 被 *args 吃掉 (5,6,7)
    kw1=8,
    kw2=9,  # 必须用关键字
    extra='a')  # 被 **kwargs 吃掉

# 输出验证：
# 仅限位置: 1, 2
# 普通参数: 3, 4
# 多余位置: (5, 6, 7)
# 仅限关键字: 8, 9
# 多余关键字: {'extra': 'a'}
```

# 形参，函数参数定义的唯一正确排列顺序如下（从左到右）：
## [仅限位置参数] → / → [位置或关键字参数]（即普通参数） → * 或 *args → [仅限关键字参数] → **kwargs
- 为了让你再也忘不掉，我画个“参数过安检”的流程图：
- 第一道门（/ 左边）：必须按顺序排队进，不能喊名字（仅限位置）。
- 第二道门（/ 和 * 之间）：既可以排队进，也可以喊名字进（普通参数）。
- 第三道门（* 右边）：必须喊名字才能进，不能插队（仅限关键字）。
- 最后的麻袋（**kwargs）：专门用来装所有喊了名字但前面没人认领的参数。

# 验证
为了严谨地验证，我们分别用 **3 种调用方式** 测试，并重点展示一个**极易踩坑的语法细节**（这正好结合了你之前学的“解包/打包”顺序）。

---

### 🧪 验证代码（直接复制运行）

```python
def demo(pos1, pos2, /, normal1, normal2, *args, kw1, kw2, **kwargs):
    print(f"仅限位置: {pos1}, {pos2}")
    print(f"普通参数: {normal1}, {normal2}")
    print(f"多余位置(args): {args}")
    print(f"仅限关键字: {kw1}, {kw2}")
    print(f"多余关键字(kwargs): {kwargs}")
    print("-" * 30)

# ---------- 验证 1：普通参数全部按位置传（你原有的例子） ----------
print("【验证1】全部按位置传递")
demo(1, 2, 3, 4, 5, 6, 7, kw1=8, kw2=9, extra='a')
# 输出：normal1=3, normal2=4, args=(5,6,7)

# ---------- 验证 2：普通参数全部按关键字传（成功！） ----------
print("【验证2】全部按关键字传递")
demo(1, 2, normal1=3, normal2=4, kw1=8, kw2=9)
# 注意：这里没有给 *args 传参，所以 args=()
# 输出：normal1=3, normal2=4, args=()

# ---------- 验证 3：混合传参（一个位置，一个关键字） ----------
print("【验证3】normal1按位置，normal2按关键字")
demo(1, 2, 3, normal2=4, kw1=8, kw2=9)
# 输出：normal1=3, normal2=4, args=()
```

**运行结果完全符合预期**，说明注释里“位置或关键字皆可”是**百分之百正确的**。

---

### ⚠️ 但在验证中，你会遇到一个“大坑”（必须警惕！）

如果你试图这样混合调用，**会直接报语法错误（SyntaxError）**：

```python
# ❌ 错误写法：关键字参数后面跟了位置参数
demo(1, 2, 3, normal2=4, 5, 6, 7, kw1=8, kw2=9)
```

**报错原因**：在 Python 的调用规则中，**一旦你使用了“关键字=值”的写法，它后面的所有参数也必须使用关键字形式**，直到遇到 `**kwargs` 的捕获范围。因为 `normal2=4` 后面的 `5,6,7` 是纯位置参数，违背了这个规则。

**这其实呼应了你之前学的“仅限关键字参数”**：虽然 `normal1` 和 `normal2` 本身是灵活的，但**调用时的顺序**依然要遵循“所有位置参数必须排在所有关键字参数之前”的铁律。

---

### 📐 横纵对比：彻底搞懂“中间地带”的边界

结合你之前的讨论，我们可以画出这条清晰的线：

| 参数名 | 位置在签名中的归属 | 能否按位置传？ | 能否按关键字传？ | 强制原因 |
| :--- | :--- | :--- | :--- | :--- |
| `pos1, pos2` | `/` **左边** | ✅ 必须 | ❌ 不行 | 语法强制（仅限位置） |
| `normal1, normal2` | `/` 和 `*args` **之间** | ✅ 可以 | ✅ 可以 | **没有任何符号限制** |
| `kw1, kw2` | `*args` **右边** | ❌ 不行 | ✅ 必须 | `*args` 充当了分隔符（强制关键字） |

> **核心结论**：`*args` 和孤零零的 `*` 一样，在参数列表中起到了**“分水岭”**的作用。它左边的 `normal1, normal2` 自由自在；它右边的 `kw1, kw2` 必须规规矩矩喊名字。

---